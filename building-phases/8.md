## Phase 8: Organizer Sales Dashboard

**Acceptance Criteria:** As an organizer I can view a dashboard for an event showing total tickets sold, revenue, active reservations, and waitlist count.

**Azure Resources to Add:**

- None _(optional: Cosmos DB `event-stats` container as a materialized view)_

**What you build:**

- `GET /events/{eventId}/dashboard` — aggregate query across orders
- Feel the RU cost of a cross-partition query
- _(Optional)_ Cosmos DB Change Feed function to maintain a pre-aggregated stats document
